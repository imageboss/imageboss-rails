language: ruby
cache: bundler
bundler_args: "--binstubs --path ../bundle --retry=3 --jobs=3"
before_install:
  - gem install bundler
  - rvm get head
  - bundle install
script:
  - bin/test
rvm:
  - 2.4.4
  - 2.3.7
  - 2.2.7
  - 2.1.10
